export interface User {
    confirmation_code: string
    confirmed: number
    email: string
    email_verified_at: string
    id: number
    last_visit: string
    name: string
    platform: string
    status: number
    verified: number
    available : number
    level_id: number
}

